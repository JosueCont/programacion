#include <iostream>
using namespace std;

string mystrcat(string c1, string c2){
	string nc = c1 += c2;
	return nc;
}

int main(){

	string str1;
	string str2;

	cout << "cadena1: "; getline(cin, str1);
	cout << "cadena2: "; getline(cin, str2);
	cout << mystrcat(str1, str2) << endl;
	return 0;
}
